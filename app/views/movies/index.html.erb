<div class="movies-grid">
  <% @movies.each do |movie| %>
    <div class="movie-card">
      <%= image_tag movie.img_url, alt: movie.title %>
      <h5><%= movie.title %></h5>
      <%= link_to "Details", movie_path(movie), class: "btn-add-watchlist" %>
    </div>
  <% end %>
</div>
